# Architectural Decision Records

This directory contains ADRs (Architectural Decision Records) — short documents capturing each non-trivial architectural choice. The index lives at `/DECISIONS.md` at the repo root.

To add an ADR: copy `template.md` to `NNNN-short-title.md`, fill it in, add to the index.
